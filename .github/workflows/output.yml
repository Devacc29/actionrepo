name: Output information
on: workflow_dispatch
jobs:
    output:
        runs-on: ubuntu-latest
        steps:
            - name: github context
              run: echo "${{ toJSON(github) }}"

           